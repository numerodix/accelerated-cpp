#include <iostream>

int main()
{
    /* This is a comment that extends over several lines
       because it uses /* and */ as its starting and endling delimiters. */
    std::cout << "Does this work?" << std::endl;
    return 0;
}